/// <reference types="expo/types" />

// NOTE: This file should not be edited. Expo regenerates it identically; it is
// committed here (rather than gitignored) so `tsc --noEmit` is deterministic in CI.
