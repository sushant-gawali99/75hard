import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

// Modal. Camera capture flow built in the Meals epic (MEAL-2/3).
export default function CaptureScreen() {
  return <ScreenPlaceholder title="Capture Meal" subtitle="Camera flow — coming soon" />;
}
