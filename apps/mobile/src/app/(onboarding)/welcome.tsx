import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

// Real Welcome + Google sign-in built in the ONB/AUTH epics.
export default function WelcomeScreen() {
  return <ScreenPlaceholder title="Welcome to Process" subtitle="Onboarding — coming soon" />;
}
